import { Data } from "../data";

const initialState = {
  shopCategories: Data.shopCategories
};

export const shopCategoriesReducer = (state = initialState, action) => {
  switch (action.type) {
      default:
        return state;
    }
    };