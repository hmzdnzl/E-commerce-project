import { Data } from "../data";
const initialState = Data.pricingFAQs;



export default function PricingReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
